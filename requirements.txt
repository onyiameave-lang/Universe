# AI Ecosystem constitutional dependencies (all optional; graceful degradation)
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
# --- optional ML backends (Forge discovers + adopts when present) ---
xgboost>=1.7.0
lightgbm>=4.0.0
torch>=2.0.0
stable-baselines3>=2.0.0
gymnasium>=0.28.0
# --- optional embeddings (Chronicle uses if present) ---
sentence-transformers>=2.2.0
# --- LLM brain (any one enables reasoning-assist) ---
anthropic>=0.25.0
openai>=1.0.0
google-genai>=0.1.0
# --- market data + trading (Oracle) ---
yfinance>=0.2.25
MetaTrader5>=5.0.45
# --- optional PDF research (Atlas) ---
pypdf>=3.0.0
# --- config + tests ---
python-dotenv>=1.0.0
pytest>=7.4.0
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
